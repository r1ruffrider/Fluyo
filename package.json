{
  "name": "fluyo-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Fluyo platform monorepo",
  "workspaces": [
    "packages/shared",
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspace @fluyo/shared && npm run build --workspace @fluyo/api && npm run build --workspace @fluyo/web",
    "dev": "npm run build --workspace @fluyo/shared && dotenv -e .env -- concurrently --kill-others-on-fail --names api,web --prefix-colors blue,magenta \"npm run dev --workspace @fluyo/api\" \"npm run dev --workspace @fluyo/web\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run lint --workspace @fluyo/shared && npm run lint --workspace @fluyo/api && npm run lint --workspace @fluyo/web",
    "postinstall": "npm run prisma:generate",
    "prisma:generate": "npm run prisma:generate --workspace @fluyo/api",
    "prisma:validate": "npm run prisma:validate --workspace @fluyo/api",
    "db:migrate": "dotenv -e .env -- npm run prisma:migrate --workspace @fluyo/api",
    "db:seed": "dotenv -e .env -- npm run prisma:seed --workspace @fluyo/api",
    "test": "npm run test --workspace @fluyo/shared && npm run test --workspace @fluyo/api",
    "typecheck": "npm run typecheck --workspace @fluyo/shared && npm run typecheck --workspace @fluyo/api && npm run typecheck --workspace @fluyo/web"
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@11.13.0",
  "devDependencies": {
    "concurrently": "10.0.3",
    "dotenv-cli": "11.0.0",
    "eslint": "9.39.5",
    "next": "16.2.10",
    "prettier": "3.9.5",
    "typescript": "5.9.3",
    "typescript-eslint": "8.64.0"
  }
}
